<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password - NRLIT</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 600px;
        margin: 20px auto;
        background-color: #ffffff;
        border-radius: 8px;
        overflow: hidden;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      }
      .header {
        background-color: #dc3545;
        color: #ffffff;
        padding: 20px;
        text-align: center;
      }
      .header h1 {
        margin: 0;
        font-size: 24px;
      }
      .content {
        padding: 20px;
      }
      .reset-message {
        font-size: 18px;
        margin-bottom: 20px;
      }
      .user-details {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 5px;
        margin-bottom: 20px;
      }
      .user-details h2 {
        margin-top: 0;
        color: #333;
        font-size: 20px;
      }
      .user-details p {
        margin: 5px 0;
        color: #555;
      }
      .reset-section {
        text-align: center;
        margin-bottom: 20px;
      }
      .reset-button {
        display: inline-block;
        background-color: #28a745;
        color: #ffffff;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 5px;
        font-weight: bold;
      }
      .reset-note {
        font-size: 14px;
        color: #777;
        margin-top: 10px;
      }
      .footer {
        background-color: #f8f9fa;
        padding: 15px;
        text-align: center;
        color: #777;
        font-size: 12px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Reset Your Password</h1>
      </div>
      <div class="content">
        <p class="reset-message">Dear {{firstName}} {{lastName}},</p>
        <p>We received a request to reset your password for your NRLIT account.
          If you made this request, please click the button below to reset your
          password.</p>

        <div class="user-details">
          <h2>Reset Request Details</h2>
          <p><strong>User ID:</strong> {{id}}</p>
          <p><strong>Email:</strong> {{email}}</p>
          <p><strong>Reset URL:</strong>
            <a href="{{resetUrl}}">{{resetUrl}}</a></p>
          <p><strong>Reset Token Expires At:</strong>
            {{resetTokenExpiresAt}}</p>
          <p><strong>Requested At:</strong> {{updatedAt}}</p>
        </div>

        <div class="reset-section">
          <a href="{{resetUrl}}" class="reset-button">Reset Your Password</a>
          <p class="reset-note">This reset link is valid until
            {{resetTokenExpiresAt}}. Please reset your password within the
            specified time.</p>
        </div>

        <p>If you did not request a password reset, please ignore this email.
          Your password will remain unchanged.</p>
        <p>If you have any questions, feel free to contact our support team.</p>
      </div>
      <div class="footer">
        <p>&copy; 2026 NRLIT. All rights reserved.</p>
        <p>This is an automated email. Please do not reply.</p>
      </div>
    </div>
  </body>
</html>